#!/usr/bin/env bash
# MOVED (2026-07-23): claude-team now lives in ~/.code/muster (v3 P0 canonical-home
# extraction). This shim keeps old PATH entries and docs working. History pre-move
# is in this repo; source of truth is muster.
exec "$HOME/.code/muster/bin/claude-team" "$@"
